Each subdirectory here describes a dataset. Contains files describing each split we're using, along with a script to download and place the dataset where it needs to be. 

The `training-inference` directory contains scripts for generating these split files as well (alongside the dataloaders for each of the datasets).